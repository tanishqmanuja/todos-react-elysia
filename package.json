{
  "name": "todos",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build"
  },
  "devDependencies": {
    "turbo": "^1.10.14"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "workspaces": [
    "client",
    "server"
  ]
}
