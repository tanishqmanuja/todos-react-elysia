{
  "name": "todos",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "format:check": "turbo format:check",
    "format:write": "turbo format:write"
  },
  "devDependencies": {
    "@biomejs/biome": "1.5.3",
    "turbo": "^1.10.14"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "workspaces": ["client", "server"]
}
